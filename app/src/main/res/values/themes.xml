<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodOrderApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FoodOrderApp" parent="Base.Theme.FoodOrderApp" />

    <!-- Light mode on -->
    <!-- BackGround if Light mode is on -->
    <style name="SCREEN">
        <item name="android:background">#FFF</item>
    </style>

    <!-- Textcolour if light mode is on-->
    <style name="TEXT">
        <item name="android:textColor">#000</item>
    </style>
    <!-- Button colour if Light mode is on -->
    <!-- Button colour if Dark mode is on -->
    <style name="BUTTON">
        <item name="android:button">@android:color/holo_orange_dark</item>
    </style>
</resources>